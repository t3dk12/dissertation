# dissertation

This repository tracks my Part C I-ReaxFF workflow in clean stages.

Commit plan:
1. Baseline I-ReaxFF snapshot
2. Initial scripts + setup + raw ffields
3. IRFF pruning/edits + script fixes
4. Converted JSONs + verification outputs
5. Active run potential (`ffield.json` from 2024vD)

## active potential for runs

`ffield.json` at the repo root is the active run potential.

It is copied from:

`ffields/li_only/2024vD_li_only.json`
