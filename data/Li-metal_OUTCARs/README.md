OUTCAR files are too large for Git.
Download them from OneDrive and place the full OUTCAR folders in this directory.

These files are only needed when rebuilding `data/traj/`.
If `data/traj/` already exists on the Linux machine, you can skip this step and go straight to training.
